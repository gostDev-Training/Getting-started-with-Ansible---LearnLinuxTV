all:
    hosts:
        ssh_server:
            ansible_host=127.0.0.1
            ansible_port=2222
            ansible_connection=ssh
            ansible_user=geo
            ansible_ssh_pass=vagrant
